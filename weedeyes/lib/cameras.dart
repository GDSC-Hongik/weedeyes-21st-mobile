import 'package:flutter/material.dart';

class Camera extends StatelessWidget {
  const Camera({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('dsdsdssdasdds');
  }
}

// 버튼을 만든다 두 개 (눌러지는지까지 확인)
// (여기서 권한 설정 만들기) 카메라 여는 거 만든다 
// (여기서 권한 설정 만들기) 갤러리 여는 거 만든다??? 
// 카메라나 갤러리에서 사진 선택하면 변수에 담기는 거 만든다
// 그 변수를 플러터 화면에 띄워본다 (이미지 띄우기)
// ML 하기
// 다 하고 위치 권한 설정 만들기
